# Нейро-блогер Instagram

Telegram-бот: Grok пишет сценарий на **o'zbek** (sport ovqat) и делает Reel 480p, ты подтверждаешь, бот кладёт в **@motivstile.ai**.

Пульт: `@Traning_with_Albert_bot`.

```bash
cd ~/neuro-blogger
./ctl.sh start
./ctl.sh logs
```

Grok: сессия grok.com (`~/.grok/auth.json`) или `XAI_API_KEY`. В ленту само не публикует — только после ✅.
